node_modules
.next
out
.env.local
.env*.local
npm-debug.log*
.DS_Store
next-env.d.ts

.vercel
.env*

# TypeScript incremental build artifact
tsconfig.tsbuildinfo

# Supabase CLI scratch state (project ref, pooler URL, versions)
supabase/.temp/

# Local render of the transactional email, not part of the site.
email-preview.html
